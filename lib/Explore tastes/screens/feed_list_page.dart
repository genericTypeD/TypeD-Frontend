import 'package:flutter/material.dart';

class FeedListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("취향 탐색"),
      ),
      body: const Center(
        child: Text("취향 탐색 페이지입니다."),
      ),
    );
  }
}
