import 'package:flutter/material.dart';

class FeedListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("취향 탐색 페이지입니다."),
    );
  }
}
